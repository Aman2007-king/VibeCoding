services:
  - type: web
    name: vibecoding
    env: node
    buildCommand: chmod +x build.sh && bash build.sh
    startCommand: npx tsx server.ts
    envVars:
      - key: NODE_ENV
        value: production
